pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        // Repositori resmi plugin Flutter
        maven { url = uri("https://storage.googleapis.com/download.flutter.io") }
    }
}

include ':app'
